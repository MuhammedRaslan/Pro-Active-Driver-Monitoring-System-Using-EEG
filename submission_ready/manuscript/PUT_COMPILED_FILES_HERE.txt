Two files belong in this folder. Neither could be produced here: main.tex is
being edited in another session and was deliberately left untouched, and this
machine has no TeX installation (pdflatex is not on PATH), so nothing could be
compiled.

  main.pdf          the compiled manuscript
  main_source.zip   main.tex + references.bib + main.bbl + the five figures
                    from ../figures/

Before compiling, apply both changes from ../00_UPLOAD_GUIDE.md:

  1. \def\revmode{clean}      (currently "track" -- yellow highlights and
                               struck-through deletions are the audit copy,
                               not the submission copy)

  2. the five \includegraphics renames listed in ../01_FIGURE_NUMBERING.md

Compile with pdfLaTeX:

  pdflatex main ; bibtex main ; pdflatex main ; pdflatex main

The PDF and the source archive must match each other exactly -- the IEEE
Author Portal checks this.
